# 本文件使用 MySQL 兼容模式的内存数据库运行骨架测试，业务 SQL 后续仍需通过真实 MySQL 集成测试。
spring:
  datasource:
    url: jdbc:h2:mem:bugloop;MODE=MySQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: false

